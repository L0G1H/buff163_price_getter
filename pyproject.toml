[project]
name = "buff163-price-getter"
version = "0.4.0"
description = "A Python module for retrieving CS2 item prices from Buff163 marketplace"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aiohttp>=3.11.11",
    "currency-convert",
    "requests>=2.32.3",
]

[tool.uv.sources]
currency-convert = { git = "https://github.com/L0G1H/currency_convert.git" }
